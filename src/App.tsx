import React from 'react';
import { BrowserRouter, Routes, Route } from 'react-router-dom';

function App() {
  return (
    <BrowserRouter>
      <React.StrictMode>
        <Routes>
          <Route />
        </Routes>
      </React.StrictMode>
    </BrowserRouter>
  );
}

export default App;
